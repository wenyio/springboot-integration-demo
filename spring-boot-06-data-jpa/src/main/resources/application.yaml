spring:
  datasource:
    url: jdbc:mysql://localhost/sb_jpa
    username: root
    password: 5211
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
    # 更新或者创建数据表结构
      ddl-auto: update
    # 控制台显示SQL
    show-sql: true